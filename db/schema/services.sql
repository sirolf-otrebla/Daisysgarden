CREATE TABLE servizi(
                id int4,
                nome varchar(255),
                immagine varchar(255),
                intro TEXT,
                quando_utile TEXT,
                telefono varchar(255),
                email varchar(255),
                week varchar(255),
                weekend varchar(255),
                PRIMARY KEY(id)
                );