CREATE TABLE servizi( 
                id int4,
                nome varchar(255),
                immagine varchar(255),
                intro TEXT,
                quando_utile TEXT,
                telefono varchar(255),
                email varchar(255),
                giorni varchar(255),
                orari varchar(255),
                PRIMARY KEY(id)
                );